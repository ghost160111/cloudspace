import { PureComponent, ReactNode } from "react";

class Home extends PureComponent {
    render(): ReactNode {
        return (
            <div style={{ height: "100dvh" }}>
                <h1>Cloudspace</h1>
            </div>
        );
    }
}

export default Home;
